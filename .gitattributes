# Explicitly declare all files to retain LF end-of-line 
#   (only \n, not \r\n), even on Windows

* text eol=lf

# Explicitly declare Feather files as binary to avoid CRLF to LF issues

*.feather binary

*.zip binary

*.ipynb linguist-vendored
